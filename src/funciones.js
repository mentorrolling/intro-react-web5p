const saludarAlumnos = () => {
  alert("Hola gentes!!");
};

const calcularNumeros = (num1, num2) => {
  return num1 + num2;
};

const ordenarAlumnos = (array) => {
  return array.sort();
};

export { saludarAlumnos, calcularNumeros, ordenarAlumnos };
